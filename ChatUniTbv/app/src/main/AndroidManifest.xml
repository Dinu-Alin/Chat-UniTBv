<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lagar.chatunitbv">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_unitbv"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_unitbv"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatUniTbv">
        <activity
            android:name=".ui.activities.MessagesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.StartActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.MainActivity" />
    </application>

</manifest>